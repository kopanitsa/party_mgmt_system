<html>
<head>
   <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
   <title>{{ event.event_name }}</title>
<head>
<body>
	hey, this is editing view.
<center>
    <div> {{ event.event_name }} </dev>
    <div> {{ event.description }} </dev>
    <br>
	<!-- list -->
    <div class="event_table"> 
    <table cellspacing="0" id="event_table_list"> 
        <thead> 
            <th> Name</th>
            {% for date in event.dates.all %}
            {% endfor %}
            <th> Comment</th>
        </thead> 
        <tbody>  
        {% for person in event.persons.all %}
            <tr>
            <td>{{ person.name }}</td>
            <td>{{ person.comment }}</td>
        {% endfor %}
        </tbody> 
    </table> 
    </div> 
    
    <!-- input form -->
    <div class="person_form" id="form">
        <form action="/event_mgmt/{{ event.id }}/{{ event.event_id }}" method="POST">
        {{ form.as_p }}
        <input type="submit" value="Submit" />
        </form>
    <br>

</center>
</body>
</html>